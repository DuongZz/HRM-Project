export const enum Shift {
  START_MORNING_SHIFT = (9 * 60 + 35) / (24 * 60),
  END_MORNING_SHIFT = (12 * 60) / (24 * 60),
  START_AFTERNOON_SHIFT = (13 * 60 + 30) / (24 * 60),
  END_AFTERNOON_SHIFT = (18 * 60) / (24 * 60),
}
